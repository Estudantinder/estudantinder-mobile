import styled from 'styled-components/native'

import { Title } from 'packages/styles'

export const NotFoundContainer = styled.View`
  flex: 1;
  width: 100%;
  justify-content: center;
  align-items: center;
  padding: 20px;
`

export const NotFoundTitle = styled(Title)`
  font-size: 16px;
  margin: 12px 0px;
  text-align: center;
`

export const NotFoundSubTitle = styled(Title)`
  font-size: 14px;
  text-align: center;
`
